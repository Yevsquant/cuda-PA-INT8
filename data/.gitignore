# Generated by benchmarks/specdec/prepare_datasets.py (regenerate any time;
# the committed manifest pins the source + seed + sampled ids). Plan §3: commit
# the manifest, not the data.
.cache/
sharegpt_subset.json
humaneval.jsonl
